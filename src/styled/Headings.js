import styled from 'styled-components';

const PageTitle = styled.h1`
  color: #505050;
  font-size: 1.4rem;
  margin: 0;
  margin-bottom: 0.6em;
`;

const HeaderTitle = styled(PageTitle)`
  color: white;
`;

export {
  PageTitle,
  HeaderTitle,
}
